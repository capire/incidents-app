// Example switchboard — activate an example by uncommenting its line below.
// Each line pulls in an overlay from the xmpls/ folder (see xmpls/README.md).

// using from '../xmpls/embeddings';   // local semantic search over incidents (@cap-js/ai)
